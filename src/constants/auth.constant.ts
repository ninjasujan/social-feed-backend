export const userNameRange = {
  min: 8,
  max: 20,
};

export const userNameError =
  'User name must be a  string with min 8 and max 20 char long';

export const passwordRange = {
  min: 8,
};

export const passwordError = 'Password should be minimum 8 char long';
